# Multilingual Text editor

The editor validates (TODO) the syntax and grammar of the text in the several languages using [LanguageTool](www.languagetool.org).

It automatically generates plain text, markdown, html, and latex files (TODO) for each language.